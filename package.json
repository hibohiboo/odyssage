{
    "name": "odyssage",
    "private": true,
    "scripts": {
      "init": "git config --local core.hooksPath .githooks && echo Changed hooks directory when npm install",
      "build": "cd packages/app && bunx run build"
    },
    "workspaces": [
        "packages/*"
    ],
    "devDependencies": {
        "prettier": "^3.3.3"
    }
}