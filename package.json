{
    "name": "odyssage",
    "private": true,
    "scripts": {
    "preinstall": "git config --local core.hooksPath .githooks && echo Changed hooks directory when npm install"
    },
    "workspaces": [
        "packages/*"
    ],
    "devDependencies": {
        "prettier": "^3.3.3"
    }
}